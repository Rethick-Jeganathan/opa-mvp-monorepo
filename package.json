{
  "name": "opa-mvp-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -w apps/mcp-server run dev",
    "start": "npm -w apps/mcp-server start"
  }
}
